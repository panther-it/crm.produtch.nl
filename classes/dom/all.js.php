<?php
  
/*
$dir = __DIR__ . "/"; 
foreach(scandir($dir) as $file)  
  if($file != "all.php" && substr($file,0,1) != "." && is_file($dir . $file)) 
    echo file_get_contents($file);
*/
require_once(__DIR__ . "/core/all.js.php");
require_once(__DIR__ . "/logging.js");
require_once(__DIR__ . "/panel.js");
require_once(__DIR__ . "/tree.js");
require_once(__DIR__ . "/treesearch.js");
?>
